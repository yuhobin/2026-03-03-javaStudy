# 📅 2026-03-30 Java_Library

### 🏷️ Topic
자바 라이브러리


### 📝 What I Learned
자주 사용 하는 라이브러리에는 java.lang / java.util / java.text / java.net / java.io / java.sql 등이 있다.

그중 클래스 집합 java.lang에 대해서 알아보았다. 너무 자주 사용되서 import를 생략하고 사용할 수 있다.

주요 메소드로는 finalize(), clone(), equals(), hashCode(), getClass() 등이 있다.

응용하기 위해 고객 데이터 끌고와서 사원목록 윈도우창 띄워보았다.

### 💡 Note
문자열 라이브러리 중 10개 정도 필수적인 메소드들을 숙지해야한다.

***equals() / compare() / ***startsWith() /  endsWith() / ***contains() /

toUpperCase() /  toLowerCase() / ***replace() / ***replaceAll() /

**length() / **trim() /  ***substring() /  ***indexOf() /

***lastIndexOf() /  ***valueOf()


<String_1 클래스 참고>
